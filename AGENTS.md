# Agent 工作规范

本文件适用于在本仓库中工作的所有 Agent。以下规则均为强制要求；若对话中的明确指令与本文件存在冲突，应先向本人说明冲突并取得确认，不得自行变更执行口径。

## 1. 开发与测试环境

1. 项目当前处于本地开发阶段，所有开发内容必须在本地完成实现。
2. 进行验证或测试时，必须通过 PHP 命令在本地启动测试环境或唤醒相关服务后再执行验证（例如使用 `php -S` 启动 PHP 本地开发服务器）。不得将远程环境视为默认开发或验证环境。

## 2. 分支与 Git 规范

1. 所有工作必须在 `new` 分支下进行。开始修改前必须确认当前分支；若不在 `new` 分支，应停止修改并先切换到 `new` 分支。
2. Agent 每次完成修改后，都必须将实际修改如实记录到 Git，并在提交信息中写明对应的修改内容与修改原因，不得遗漏、隐瞒或使用含糊的提交说明。
3. 每次任务或对话结束前，必须检查 Git 状态，并确保工作区与暂存区均处于干净状态，不得遗留未提交或未处理的修改。

## 3. 参考对象

`bbs-new` 分支属于此前的遗留产物，可在需要时作为实现参考，但不得直接将其视为当前需求或最终标准。实际工作应以当前对话中的命令和要求为主。

## 4. 文件修改权限

对 `api/` 目录及其下所有 API 文件的任何修改，必须遵循以下流程：

1. 仅在本人明确指示需要修改 API 文件时，Agent 才可提出相关修改方案。
2. 修改前，Agent 必须声明拟修改的文件、代码范围、预期行为及修改原因。
3. 只有在本人明确确认上述修改范围后，Agent 才能继续执行。
4. 未获得确认前，不得编辑、格式化、移动、删除或以其他方式改动任何 API 文件。
