# Agent 工作规范

本文件适用于在本仓库中工作的所有 Agent。以下规则均为强制要求；若对话中的明确指令与本文件存在冲突，应先向本人说明冲突并取得确认，不得自行变更执行口径。

## 1. 开发与测试环境

1. 项目当前处于本地开发阶段，所有开发内容必须在本地完成实现。
2. 进行验证或测试时，必须通过 PHP 命令在本地启动测试环境或唤醒相关服务后再执行验证（例如使用 `php -S` 启动 PHP 本地开发服务器）。不得将远程环境视为默认开发或验证环境。
3. 界面与 UI 的视觉验证统一由本人负责。除非本人在当前对话中明确要求，否则 Agent 不得自行执行截图检查、像素对比、浏览器视觉审查或视觉回归，也不得以自行作出的视觉判断替代本人的验收。类型检查、构建检查及不依赖视觉判断的功能验证不受此限制。总之，除非本人明确要求，否则 Agent 不得自行打开任何浏览器或应用程序。

## 2. 分支与 Git 规范

1. 所有工作必须在 `new` 分支下进行。开始修改前必须确认当前分支；若不在 `new` 分支，应停止修改并先切换到 `new` 分支。
2. Agent 每次完成修改后，都必须将实际修改如实记录到 Git，并在提交信息中写明对应的修改内容与修改原因，不得遗漏、隐瞒或使用含糊的提交说明。
3. 每次任务或对话结束前，必须检查 Git 状态，并确保工作区与暂存区均处于干净状态，不得遗留未提交或未处理的修改。

4. 一次性维护脚本、数据处理脚本等本地工具统一放在 `tool/` 目录，不纳入 Git 跟踪；需要部署时由本人在服务器上单独上传或执行。

## 3. 参考对象

`bbs-new` 分支属于此前的遗留产物，可在需要时作为实现参考，但不得直接将其视为当前需求或最终标准。实际工作应以当前对话中的命令和要求为主。

为避免在开发过程中反复切换分支，`bbs-new` 已以 detached worktree 的形式检出到 `/Users/mac/Code/CAPUBBS-bbs-new-reference`。Agent 应优先从该目录读取遗留实现作为参考，不得在参考目录内修改、提交或开展当前需求的开发；当前需求的所有变更仍须在 `/Users/mac/Code/CAPUBBS` 的 `new` 分支完成。

## 4. 文件修改权限

对 `api/` 目录及其下所有 API 文件的任何修改，必须遵循以下流程：

1. 仅在本人明确指示需要修改 API 文件时，Agent 才可提出相关修改方案。
2. 修改前，Agent 必须声明拟修改的文件、代码范围、预期行为及修改原因。
3. 只有在本人明确确认上述修改范围后，Agent 才能继续执行。
4. 未获得确认前，不得编辑、格式化、移动、删除或以其他方式改动任何 API 文件。

## 5. 界面文案规范

1. 除非本人在当前需求中明确声明需要，否则界面中不得自行添加任何副标题、说明性辅助文案或演示提示。
2. 设计原则：
   - 界面文案应简洁、清晰，避免冗余或重复。
   - 文案应与功能直接相关，避免引入不必要的解释或背景信息。
   - 避免使用模糊或不明确的词汇，确保用户能够快速理解操作意图。

## 6. 新旧论坛范围

1. 当前需求默认针对新论坛，相关页面和组件位于 `forum/` 目录。
2. 除非本人明确要求兼容或修改旧论坛，否则不得修改 `bbs/` 目录及旧论坛入口；新论坛与旧论坛的实现、文案和验证应严格区分。

## 7. 签名档与嵌入 HTML 渲染原则

1. 签名档中的 `[post]` 引用楼层默认需要完整保留并执行其原始 HTML、CSS 和脚本；这是既定功能，不得擅自改为纯文本、摘要或静态安全片段。
2. 处理包含顶层 `<style>`、`<script>` 或其他可执行嵌入内容的 HTML 片段时，必须使用 HTML fragment / `template` / `DocumentFragment` 语义；不得使用完整文档解析后仅返回 `body.innerHTML`，以免浏览器将顶层节点提升到临时 `head` 后丢失。
3. 任何签名档或嵌入楼层渲染方式、隔离方式、样式继承规则的修改，必须保持原始嵌入样式与脚本行为，并通过本地 PHP 测试环境针对真实签名档数据验证；未经本人明确要求，不得为方便实现而删减、屏蔽或重解释上述行为。

## 8. 本地与测试账号约定

1. 本地开发环境和测试服务器的所有账号统一使用密码 `123456`，可在命令行 API 验证或测试操作中使用。
2. 该密码约定仅限本地开发环境和测试服务器，禁止用于生产环境，也不得写入生产配置、日志或对外文档。
